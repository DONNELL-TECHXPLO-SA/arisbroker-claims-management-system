# Diagrams

Architecture, sequence, and flow diagrams live here. Prefer text-based formats (Mermaid in `.md` files) so they stay diffable in git; use exported images only when a tool doesn't support that.
